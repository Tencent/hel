import * as arr from './arr';
import * as file from './file';
import * as obj from './obj';
import * as slash from './slash';
import * as str from './str';

export { arr, obj, slash, str, file };

export default {
  arr,
  obj,
  slash,
  str,
  file,
};
