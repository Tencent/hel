import * as apis from './apis';
import * as ins from './ins';

export const {
  getMayStaticShadowNode
} = apis;
export const {
  createInstance,
} = ins;

export default {
  getMayStaticShadowNode,
  createInstance,
};
